---
docker_tls: true
docker_ca: "{{ lookup('file', '/tmp/tls_certs/caCert.pem') }}"
docker_cert: "{{ lookup('file', '/tmp/tls_certs/serverCert.pem') }}"
docker_key: "{{ lookup('file', '/tmp/tls_certs/serverKey.pem') }}"
