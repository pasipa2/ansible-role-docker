---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - pipenv run molecule test
  # - name: test
  #   commands:
  #     - make build
  
