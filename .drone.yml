---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - pipenv install --python /usr/bin/python
  #     - make prepare
  # - name: test
  #   commands:
  #     - make build
  