---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - export PATH=$PATH:/home/drone/miniconda3/bin
      - molecule test
  # - name: test
  #   commands:
  #     - make build
  
