---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: test
    commands:
      - export PATH=$PATH:/home/drone/miniconda3/bin
      - ls
  # - name: test
  #   commands:
  #     - make build
  
