---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - id 
  #     - make prepare
  # - name: test
  #   commands:
  #     - make build
  