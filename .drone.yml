---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - pipenv install 
      - make prepare
  - name: test
    commands:
      - make build
  