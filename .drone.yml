---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: prepare
    commands:
      - /home/drone/miniconda3/bin/molecule test
  # - name: test
  #   commands:
  #     - make build
  
